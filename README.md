# secure-api-request
A Python project demonstrating secure API authentication using environment variables, Bearer Token authorization, and the Requests library. It showcases professional practices such as loading secrets from a .env file, sending authenticated HTTP requests, handling API responses, managing errors, and protecting sensitive credentials with .gitignore.
